# StarfishExchange

To install:

```bash
$ git clone
$ cd StarfishExchange
$ python3 -m venv .venv
$ source .venv/bin/activate
$ pip install -r requirements.txt
```

To run locally:

```bash
$ python main.py --reload
```
